# 教材編集ガイド

このドキュメントは、教材を編集・追加する際のルールをまとめたものです。

---

## 画像・動画ファイルの配置規則

### 配置先ディレクトリ

```
docs/public/images/
├── common/      # OS共通の図解・説明画像
├── windows/     # Windows固有の操作画面
├── macos/       # macOS固有の操作画面（将来）
└── linux/       # Linux固有の操作画面（将来）
```

Markdownから参照するときは `/latex-course/images/windows/xxx.png` のように `base` を含めずに書く（VitePressが自動補完します）。

実際には `![説明文](/images/windows/xxx.png)` と書けばよい。

### ファイル名の規則

```
{章}-{ページ}-{説明}.{拡張子}
```

**例：**

| ファイル名 | 内容 |
|---|---|
| `02-texlive-download.png` | 第2章 / TeX Liveのダウンロード操作 |
| `02-vscode-install-complete.png` | 第2章 / VS Codeインストール完了画面 |
| `02-latex-workshop-search.png` | 第2章 / LaTeX Workshop検索画面 |
| `02-verify-pdf-output.png` | 第2章 / PDFの出力確認 |

- 半角英数字とハイフンのみ使用する
- 日本語・スペース・アンダースコアは使わない

### アニメーション・動画の形式

| 形式 | 用途 |
|---|---|
| WebP（アニメーション） | 短い操作手順（推奨、ファイルサイズが小さい） |
| GIF | 上記が使えない環境向けの代替 |
| MP4 | 長い操作（30秒以上）や音声を含む場合 |

ファイル名の規則は静止画と同じ。拡張子だけ変わる（例：`02-texlive-install.webp`）。

---

## TODOコメントの書き方

撮影待ちの画像・動画は、本文中に次の形式でTODOコメントを置く。

```markdown
<!-- TODO(media): ここで何の操作を撮影するか -->
```

**例：**

```markdown
<!-- TODO(media): TeX Live公式サイトを開いてダウンロードリンクをクリックするまでの操作 -->
<!-- TODO(media): VS CodeでLaTeX Workshopを検索してインストールする操作 -->
<!-- TODO(media): test.texを保存してPDFが生成される操作 -->
```

- `TODO(media):` のフォーマットは統一する（TODO一覧の抽出に使う）
- コメントは本文中の「ここに画像・動画が入る」箇所の直下に置く
- 一行で操作内容が分かるように書く

---

## ページ執筆の原則

各ページの構成は次を基本とする。

1. **このページでできること**（箇条書き、2〜4項目）
2. 本文（操作手順は番号付き、`1.` `2.` ...）
3. **ここまでできればOK**（チェックリスト形式）
4. **よくある問題**（必要な場合のみ）
5. **次のステップ**（次ページへのリンク）

---

## 未完成ページへの対処

macOS・Linux・Overleafなど準備中のページには次のブロックを冒頭に置く。

```markdown
::: warning 準備中
この章はまだ準備中です。しばらくお待ちください。
:::
```
